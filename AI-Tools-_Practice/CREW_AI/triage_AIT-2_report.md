# Triage Report — Bug ID: AIT-2

**Title:** VWO Login - Invalid credentials error not displaying properly

## 1. Verification Test (Smoke)
Playwright test to verify error message display for invalid login credentials:

```typescript
import { test, expect } from '@playwright/test';

test('should display error message for invalid login credentials', async ({ page }) => {
  await page.goto('https://app.vwo.com/login');
  await page.fill('input[name="email"]', 'wronguser@example.com');
  await page.fill('input[name="password"]', 'wrongpassword123');
  await page.click('button[type="submit"]');
  await expect(page.locator('text="Invalid email"')).toBeVisible();
  await expect(page.locator('text="Invalid password"')).toBeVisible();
});
```

## 2. Regression Test Cases
1. Login with correct email and incorrect password — expect password error.
2. Login with incorrect email and correct password — expect email error.
3. Login with both email and password empty — expect required field errors.
4. Login with correct credentials after previous invalid attempt — expect successful login.
5. Concurrent login attempts with invalid credentials — each should show appropriate error.

## 3. Edge Cases
- Very long email or password (>100 chars) — ensure no crashes and correct error handling.
- SQL injection-like inputs (e.g., `' OR 1=1 --`) — should be rejected safely.
- Special characters in email/password — handled appropriately.

## 4. Suggested Test Automation Approach
Use Playwright with TypeScript. Example edge-case test:

```typescript
import { test, expect } from '@playwright/test';

test('should handle very long email', async ({ page }) => {
  const veryLongEmail = 'a'.repeat(100) + '@example.com';
  await page.goto('https://app.vwo.com/login');
  await page.fill('input[name="email"]', veryLongEmail);
  await page.fill('input[name="password"]', 'password123');
  await page.click('button[type="submit"]');
  await expect(page.locator('text="Invalid email"')).toBeVisible();
});
```

## 5. Load/Performance Tests
- Scenario: 1000 concurrent users attempting logins (mix of valid/invalid) over 5 minutes.
- Goals: average response < 2s, <5% error rate, correct error messages for invalid attempts.

---

_Report generated by QA Bug Triage Crew (CREW_AI)._
