Act as an expert Senior QA Automation Engineer and Software Developer specializing in the Model Context Protocol (MCP). 

I need you to write a complete Python script named `server.py` using the `fastmcp` library to build a custom MCP server for "TheTestingAcademy". The server must fully expose a specific set of tools, resources, data models, and prompts so it can be completely inspected using the MCP Inspector.

Follow these exact architectural requirements:

### 1. Server Configuration
- Initialize the server using the `FastMCP` class.
- Name the server instance: "TheTestingAcademy_Automation_Server"

### 2. Tools Requirement (Create exactly 20 Dummy Playwright Tools)
Create exactly 20 distinct tools decorated with `@mcp.tool()`. Every single tool must simulate a common Playwright browser automation action. For example, include tools like `browser_to_url`, `click_element`, `fill_input`, `take_screenshot`, `wait_for_selector`, etc. 
- Each tool must accept relevant dummy parameters (e.g., `url`, `selector`, `text`).
- Each tool must print or return a clear status string starting with the format: "Ok started: [action description]" (e.g., "Ok started: Navigating to https://example.com").

### 3. Resources & Data Requirement (Create exactly 5 items total: 3 Text Resources, 2 Structured Data Resources)
Expose these using the `@mcp.resource()` decorator with unique URI schemas:
- **3 Context/Log Resources:**
  1. `artifacts://logs/latest` - Returns raw string text logs of a mock test execution run.
  2. `config://env/staging` - Returns base configuration properties text.
  3. `reports://regression/summary` - Returns a text overview summary of a test suite run.
- **2 Structured Data Resources:**
  4. `data://suite/metrics` - Returns structured metrics (Total: 51, Passed: 37, Failed: 14, Pass Rate: 72.55%).
  5. `data://dom/schema` - Returns a mockup JSON string representing a localized page element schema hierarchy.

### 4. Prompts Requirement (Create exactly 5 Prompts)
Create exactly 5 smart prompt templates decorated with `@mcp.prompt()`. These act as shortcuts to help an AI agent construct QA engineering workflows:
1. `generate_smoke_test(url: str)` - Template asking to write a basic availability verification test.
2. `debug_locator_failure(error_log: str)` - Template to analyze stack traces and find broken selectors.
3. `optimize_test_steps(raw_steps: str)` - Template to refactor verbose step records down to 1-2 core steps.
4. `create_bug_report(failed_step: str)` - Template to output a structured Jira bug format from a broken run.
5. `suggest_wait_strategy(behavior: str)` - Template to generate explicit element state wait models.

### 5. Execution Execution block
Ensure the script includes the standard `if __name__ == "__main__": mcp.run()` entry point so it boots seamlessly over standard input/output (stdio) transport layers.

Provide clean, standalone, well-commented Python code with no missing implementations or shortcuts.